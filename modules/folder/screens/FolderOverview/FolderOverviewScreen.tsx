import useStyles from "./styled";

const FolderOverviewScreen = () => {
	const { styles } = useStyles();

	return <div className={styles.wrapper}>FolderOverviewScreen</div>;
};

export default FolderOverviewScreen;
